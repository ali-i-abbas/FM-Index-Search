This will run the FM Index Search with the specified pattern in pattern.txt.

Compile the code using this command:

javac *.java

Copy the desired file to the current path (For example the files in EvaluationFiles directory), modify the pattern.txt with the desired pattern and run this:

java FMIndexSearch <text file name> pattern.txt

The results will be shown on screen and in results.txt file. 

